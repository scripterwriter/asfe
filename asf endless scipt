local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "AFSE | #1 Script",
    LoadingTitle = "AFSE",
    LoadingSubtitle = "by VisualDoggyStudios",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "AFSE",
        FileName = "Config"
    },
    Discord = {
        Enabled = true,
        Invite = "bZv3Ceagqu",
        RememberJoins = true
    },
    KeySystem = false
})

local AutofarmTab = Window:CreateTab("Autofarm", "dumbbell")
local FruitsTab = Window:CreateTab("Fruits", "apple")
local MobsTab = Window:CreateTab("Mobs", "swords")
local ChikaraTab = Window:CreateTab("Chikara", "gift")
local TeleportsTab = Window:CreateTab("Teleports", "map-pin")
local EspTab = Window:CreateTab("ESP", "eye")
local CodesTab = Window:CreateTab("Codes", "ticket")
local MiscTab = Window:CreateTab("Misc", "package")
local SettingsTab = Window:CreateTab("Settings", "settings")
local ExtrasTab = Window:CreateTab("Extras", "crown")

Rayfield:Notify({
    Title = "AFSE Loaded!",
    Content = "All features ready.\nDiscord invite copied.\nGo dominate the game!",
    Duration = 8
})

setclipboard("https://discord.gg/bZv3Ceagqu")

-- Autofarm Paragraph (Rayfield supports this)
AutofarmTab:CreateParagraph({
    Title = "Autofarm Features",
    Content = "Toggle the features below to enable or disable automatic actions."
})

-- Correct remote path for Anime Fighting Simulator: Endless (based on common structure)
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TrainRemote = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Train")  -- Standard path

-- Auto Train Strength
AutofarmTab:CreateToggle({
    Name = "Auto Train Strength",
    CurrentValue = false,
    Flag = "AutoStrength",  -- Unique flag for saving
    Callback = function(Value)
        while Value do
            TrainRemote:FireServer(1)
            task.wait(0.1)
        end
    end
})

-- Auto Farm Durability
AutofarmTab:CreateToggle({
    Name = "Auto Farm Durability",
    CurrentValue = false,
    Flag = "AutoDurability",
    Callback = function(Value)
        while Value do
            TrainRemote:FireServer(2)
            task.wait(0.1)
        end
    end
})

-- Auto Farm Chakra
AutofarmTab:CreateToggle({
    Name = "Auto Farm Chakra",
    CurrentValue = false,
    Flag = "AutoChakra",
    Callback = function(Value)
        while Value do
            TrainRemote:FireServer(3)
            task.wait(0.1)
        end
    end
})

-- Auto Farm Sword
AutofarmTab:CreateToggle({
    Name = "Auto Farm Sword",
    CurrentValue = false,
    Flag = "AutoSword",
    Callback = function(Value)
        while Value do
            TrainRemote:FireServer(4)
            task.wait(0.1)
        end
    end
})

-- Auto Farm Speed & Agility
AutofarmTab:CreateToggle({
    Name = "Auto Farm Speed & Agility",
    CurrentValue = false,
    Flag = "AutoSpeedAgility",
    Callback = function(Value)
        while Value do
            TrainRemote:FireServer(5)  -- Speed
            TrainRemote:FireServer(6)  -- Agility
            task.wait(0.1)
        end
    end
})

-- Theme Dropdown
SettingsTab:CreateDropdown({
    Name = "UI Theme",
    Options = {"Default", "Dark", "Light", "Aqua", "Amethyst", "Rose"},
    CurrentOption = "Dark",
    Flag = "UITheme",
    Callback = function(Option)
        Rayfield.Library:ChangeTheme(Option)  -- Correct method to change theme
    end
})

-- Load external modules (repeat for each)
local TabsTable = {
    Autofarm = AutofarmTab,
    Fruits = FruitsTab,
    Mobs = MobsTab,
    Chikara = ChikaraTab,
    Teleports = TeleportsTab,
    ESP = EspTab,
    Codes = CodesTab,
    Misc = MiscTab,
    Extras = ExtrasTab,
    Settings = SettingsTab
}

task.spawn(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisualDoggyStudios/Anime-Fighting-Simulator-Endless/refs/heads/main/obfuscated_autofarm.lua"))()(TabsTable, Rayfield)
    end)
end)

task.spawn(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisualDoggyStudios/Anime-Fighting-Simulator-Endless/refs/heads/main/obfuscated_fruits.lua"))()(TabsTable, Rayfield)
    end)
end)

-- Repeat the above task.spawn for mobs.lua, chikara.lua, teleports.lua, ESP.lua, codes.lua, misc.lua

if ExtrasTab then
    task.spawn(function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/VisualDoggyStudios/ScriptHub/refs/heads/main/Extras.lua"))()(TabsTable, Rayfield)
        end)
    end)
end)
